from singleaddress import SingleAddress
class Addressbook(SingleAddress):
    def __init__(self,address):
        self.adress=address
        def details(self):
            if(self.address==s.firstname()):
                raise Exception("DiplicatAddressException")